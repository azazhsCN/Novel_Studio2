# Novel_Studio2 项目需求文档

> 本文档是项目的权威需求参考。所有功能变更、BUG修复、需求补充均应记录在此。

---

## 一、项目定位

小说写作智能体工作台 —— Web界面，Python全栈，统一管理多本小说的全流程写作平台。

用户之前手工+AI辅助写小说，效率低且容易出现剧情冲突。本项目旨在自动化写作流程并提供审计保障。

---

## 二、技术栈

| 组件 | 技术选型 | 说明 |
|------|----------|------|
| 后端 | Python FastAPI | 异步高性能Web框架 |
| 前端 | FastAPI + 轻量HTML/JS | 单页应用，暗色主题 |
| 存储 | 本地文件系统 + JSON | 项目数据以目录+JSON文件存储 |
| API层 | 可配置模块 | config.yaml配置，支持切换AI服务 |
| 默认API | deepseek-v4-flash | 1000K上下文，384K最大输出，OpenAI兼容接口 |

---

## 三、核心功能模块

### 3.1 小说项目管理（P0）

- 创建/编辑/删除小说项目
- 每个项目独立目录结构
- 项目配置：基准字数、重点章节倍率、转折章节倍率

### 3.2 结构化核心提示词（P0）

将传统的"一大坨"提示词拆解为5个独立模块：

| 模块 | 内容 | 更新频率 |
|------|------|----------|
| 基础设定 | 世界观、背景、核心规则、特殊设定 | 低频（项目创建时） |
| 角色卡片 | 每个角色独立卡片：姓名、年龄、外貌、性格、关系、当前状态、弧线 | 中频（角色变化时） |
| 剧情概述 | 已写章节的梗概，根据总篇幅自动调整颗粒度 | 高频（每批章节后） |
| 文风设定 | 叙述视角、语言风格、描写特点、结构特点 | 低频（基本固定） |
| 续写方向 | 当前剧情走向、冲突线索、注意事项 | 中频（规划时更新） |

**剧情概述自动颗粒度逻辑：**
- 0-50章：每章详细摘要（200-300字/章）
- 50-150章：早期章节压缩为一段，近期章节保留详细摘要
- 150+章：早期章节压缩为一句话，中期压缩为一段，近期保留详细
- 用户可手动锁定某些"关键章节"不被压缩

### 3.3 导入已有小说（P0）

- 上传已有小说txt文件
- AI分析小说内容，自动生成：基础设定、角色卡片、剧情概述、文风设定、续写方向
- 人工审核/修改AI生成的结构化信息
- 确认后创建项目，可直接进入续写

### 3.4 章节规划生成（P0）

- 输入方向提示词影响规划走向（如"不要增加新人物"、"这10章要有转折"）
- 可自定义规划章节数（非固定10章）
- AI自动生成每章的：标题、类型、字数、时间、场景、核心情节、写作提示词
- 章节规划支持增删改查（CRUD）
- 规划中可手动编辑单个章节的所有字段

#### 章节类型系统

| 类型 | 字数系数 | 说明 |
|------|----------|------|
| 普通章节 | 100% | 基准字数，用户可配置（默认3000字） |
| 重点章节 | 130% | 关键情节、重要场景 |
| 转折章节 | 150% | 剧情转折、高潮段落 |

### 3.5 正文续写生成（P0）

- 根据核心提示词 + 上一章内容 + 章节规划，调用AI逐章生成正文
- 按章节类型自动匹配目标字数
- 可附加文风样本（独立管理，按需挂载）
- 生成后支持修改意见迭代：输入修改意见 → AI重新生成
- 支持多次迭代直到满意
- 定稿后保存为纯文本txt文件

### 3.6 核心提示词自动更新（P0）

- 写完一批章节后，将新章节内容融入核心提示词
- 剧情概述按颗粒度规则自动压缩
- 角色卡片自动更新状态变化
- 人工审核后确认覆盖

### 3.7 审计系统（P1）

#### 第一层：资源追踪器（自动维护）

系统自动从小说内容中提取并维护一张"资源表"：

| 类别 | 示例 |
|------|------|
| 财富/资产 | 深渊俱乐部（五层别墅）、筱惠银行账户（120万欧元） |
| 重要物品 | 腹部"月"字烙印、山田组长赠予的短刀 |
| 系统/数值 | 尼克积分、柳冰冰堕落值（适用于系统流小说） |
| 人物状态 | 筱惠：怀孕中、小雪：精神崩溃状态 |
| 伏笔/悬念 | 山田组长的"勿忘根本"暗示 [未收] |

#### 第二层：冲突检测（每章完成后自动运行）

| 检测类型 | 说明 |
|----------|------|
| 时间线冲突 | 角色在同一时间出现在两个地方？ |
| 人物状态冲突 | 已死/已离开的角色再次出现？ |
| 物品冲突 | 已销毁的物品再次使用？ |
| 设定冲突 | 与基础设定矛盾？ |
| 数值冲突 | 财富/能力数值异常变化？ |
| 伏笔冲突 | 已收伏笔再次埋设？ |

#### 第三层：审计报告

每章写完后生成审计报告，包含：通过项、需确认项（附操作按钮）、资源变动。

用户可选择：忽略 / 更新资源表 / 修改章节。

### 3.8 写作进度统计（P1）

仪表盘展示：
- 已定稿章节数
- 已规划章节数
- 总字数
- 本批进度

### 3.9 文风样本管理（P1）

- 独立管理文风样本文件
- 按需附加到写作提示词中
- 支持上传多个样本文件

### 3.10 API配置管理（P1）

config.yaml 可配置：
- API端点（base_url）
- API密钥（api_key）
- 模型名称（model）
- 最大输出tokens（max_tokens）
- 温度参数（temperature）
- 系统提示词（system_prompt）

---

## 四、工作流程

```
[项目创建]
  方式A：手动创建 → 填写各模块信息
  方式B：导入已有小说 → AI分析生成 → 人工审核

[写作准备]
  加载项目 → 检查/编辑核心提示词各模块 → 选择附加文风样本

[章节规划]
  输入方向提示词 → 选择规划章节数 → 调用AI生成 →
  人工审核/增删改 → 确认保存

[正文写作]
  选择要写的章节 → 调用AI生成正文 → 人工审核 →
  不满意？输入修改意见 → 重新生成 → 满意？保存为txt

[审计]
  每章写完 → 调用AI审计 → 查看报告 → 解决冲突 → 更新资源表

[核心更新]
  选中已写章节 → AI自动更新各模块 → 人工审核 → 确认
  （剧情概述自动按颗粒度规则压缩）
```

---

## 五、数据目录结构

```
Novel_Studio2/
├── start.bat                  # 一键启动脚本
├── run.py                     # Python启动入口
├── config.yaml                # API配置
├── requirements.txt           # Python依赖
├── 项目需求.md                # 本文档
├── 开发日志.md                # 开发记录
├── app/                       # 应用代码
│   ├── main.py               # FastAPI主应用
│   ├── api/                  # API路由
│   │   ├── novels.py         # 小说项目管理
│   │   ├── chapters.py       # 章节规划与写作
│   │   └── audit.py          # 审计系统
│   ├── core/                 # 核心逻辑
│   │   ├── config.py         # 配置加载
│   │   ├── api_client.py     # API客户端
│   │   ├── prompt_builder.py # 提示词构建
│   │   ├── importer.py       # 导入分析
│   │   ├── planner.py        # 章节规划
│   │   ├── writer.py         # 正文写作
│   │   └── auditor.py        # 审计系统
│   ├── models/               # 数据模型
│   │   ├── novel.py          # 小说项目
│   │   ├── chapter.py        # 章节
│   │   └── resource.py       # 资源追踪
│   └── templates/            # 前端模板
│       └── index.html        # Web界面
├── data/                      # 小说数据（运行时生成）
│   └── novel_xxx/
│       ├── project.json      # 项目配置
│       ├── resources.json    # 资源追踪表
│       ├── core_prompt/      # 核心提示词模块
│       ├── style_samples/    # 文风样本
│       ├── plans/            # 章节规划JSON
│       └── chapters/         # 已写章节txt + 元数据json
└── 小说样本/                  # 用户提供的参考样本
    ├── 沉沦/
    └── 黑人/
```

---

## 六、API端点一览

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/novels` | 获取所有小说项目 |
| POST | `/api/novels` | 创建新项目 |
| GET | `/api/novels/{id}` | 获取项目详情 |
| PUT | `/api/novels/{id}` | 更新项目配置 |
| DELETE | `/api/novels/{id}` | 删除项目 |
| GET | `/api/novels/{id}/core-prompt` | 获取核心提示词 |
| PUT | `/api/novels/{id}/core-prompt` | 更新核心提示词 |
| POST | `/api/novels/{id}/import` | 导入小说文件分析 |
| POST | `/api/novels/{id}/style-sample` | 上传文风样本 |
| GET | `/api/novels/{id}/chapters/plans` | 获取所有规划 |
| POST | `/api/novels/{id}/chapters/plans/generate` | AI生成规划 |
| GET | `/api/novels/{id}/chapters/plans/{plan_id}` | 获取单个规划 |
| PUT | `/api/novels/{id}/chapters/plans/{plan_id}` | 更新规划 |
| DELETE | `/api/novels/{id}/chapters/plans/{plan_id}` | 删除规划 |
| PUT | `/api/novels/{id}/chapters/plans/{plan_id}/chapters/{num}` | 编辑单章规划 |
| GET | `/api/novels/{id}/chapters` | 获取所有已写章节 |
| POST | `/api/novels/{id}/chapters/write` | AI生成正文 |
| GET | `/api/novels/{id}/chapters/{num}` | 获取章节内容 |
| POST | `/api/novels/{id}/chapters/{num}/revise` | 修改重写 |
| PUT | `/api/novels/{id}/chapters/{num}/finalize` | 定稿/取消定稿 |
| POST | `/api/novels/{id}/chapters/{num}/audit` | 审计章节 |
| GET | `/api/novels/{id}/audit/report` | 审计报告 |
| GET | `/api/novels/{id}/audit/resources` | 资源追踪表 |
| POST | `/api/novels/{id}/audit/resolve` | 解决冲突 |

---

## 七、需求变更记录

> 后续所有需求变更、补充、BUG修复在此追加。

| 日期 | 类型 | 内容 | 状态 |
|------|------|------|------|
| 2026-06-01 | 初始 | 完成全部P0/P1功能模块搭建 | ✅ 已完成 |
| 2026-06-01 | 功能 | 导入创建已定稿章节、流式AI输出、审计markdown渲染、写作独立工作台、角色卡片折叠编辑 | ✅ 已完成 |
| 2026-06-01 | 功能 | 剧情概述增量缓存+分段处理、导出中文文件名(RFC 5987)、自动检测起始章节、单章规划删除、loading overlay | ✅ 已完成 |
| 2026-06-01 | 功能 | 手动编辑保存按钮、文风样本状态显示、审计资源手动编辑、统计逻辑调整(待写) | ✅ 已完成 |
| 2026-06-02 | 安全 | 全面代码审查+23项修复：路径遍历防护、XSS修复、上传安全、API key环境变量、非原子写入、or-bug、配置缓存等 | ✅ 已完成 |

---

## 八、待实现/优化项

| 优先级 | 内容 | 状态 |
|--------|------|------|
| P2 | 已写章节批量操作（批量定稿、批量审计） | 待实现 |
| P2 | 前端文件拆分（当前~1700行单HTML） | 待实现 |
| P2 | 前端交互优化（拖拽排序规划等） | 待实现 |
| P3 | 多语言/多API对比 | 待评估 |

---

## 九、API端点完整一览（截至2026-06-02）

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/health` | 健康检查 |
| GET | `/api/novels` | 获取所有小说项目 |
| POST | `/api/novels` | 创建新项目（id需符合 `^[a-zA-Z0-9_-]{1,50}$`） |
| GET | `/api/novels/{id}` | 获取项目详情（含total_word_count、total_written） |
| PUT | `/api/novels/{id}` | 更新项目配置 |
| DELETE | `/api/novels/{id}` | 删除项目 |
| GET | `/api/novels/{id}/core-prompt` | 获取核心提示词 |
| PUT | `/api/novels/{id}/core-prompt` | 更新核心提示词 |
| POST | `/api/novels/{id}/import` | 导入小说文件分析（50MB限制） |
| POST | `/api/novels/{id}/style-sample` | 上传文风样本（50MB限制，文件名清洗） |
| GET | `/api/novels/{id}/style-samples` | 获取文风样本列表 |
| POST | `/api/novels/{id}/ai-update` | AI更新角色/剧情概述 |
| GET | `/api/novels/{id}/export/chapter/{num}` | 单章导出txt |
| GET | `/api/novels/{id}/export/all` | 全书导出txt |
| GET | `/api/novels/{id}/chapters/plans` | 获取所有规划 |
| GET | `/api/novels/{id}/chapters/plans/next-chapter` | 获取下一个可用章节号 |
| POST | `/api/novels/{id}/chapters/plans/generate` | AI生成规划 |
| GET | `/api/novels/{id}/chapters/plans/{plan_id}` | 获取单个规划详情 |
| PUT | `/api/novels/{id}/chapters/plans/{plan_id}` | 更新规划 |
| DELETE | `/api/novels/{id}/chapters/plans/{plan_id}` | 删除规划 |
| PUT | `/api/novels/{id}/chapters/plans/{plan_id}/chapters/{num}` | 编辑单章规划 |
| DELETE | `/api/novels/{id}/chapters/plans/{plan_id}/chapters/{num}` | 删除单章规划 |
| GET | `/api/novels/{id}/chapters` | 获取所有已写章节 |
| POST | `/api/novels/{id}/chapters/write` | AI生成正文（非流式） |
| POST | `/api/novels/{id}/chapters/write/stream` | AI生成正文（SSE流式） |
| GET | `/api/novels/{id}/chapters/{num}` | 获取章节内容 |
| PUT | `/api/novels/{id}/chapters/{num}/content` | 手动更新章节内容 |
| POST | `/api/novels/{id}/chapters/{num}/revise` | 修改重写（非流式） |
| POST | `/api/novels/{id}/chapters/{num}/revise/stream` | 修改重写（SSE流式） |
| PUT | `/api/novels/{id}/chapters/{num}/finalize` | 定稿/取消定稿 |
| POST | `/api/novels/{id}/chapters/{num}/audit` | 审计章节 |
| GET | `/api/novels/{id}/audit/report` | 审计报告 |
| GET | `/api/novels/{id}/audit/resources` | 资源追踪表 |
| PUT | `/api/novels/{id}/audit/resources/{category}/{index}` | 编辑资源项 |
| DELETE | `/api/novels/{id}/audit/resources/{category}/{index}` | 删除资源项 |
| POST | `/api/novels/{id}/audit/resolve` | 解决冲突 |
