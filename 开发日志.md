# Novel_Studio2 开发日志

> 记录每次开发做了什么，方便复盘和回溯。

---

## 2026-06-01 — 项目初始化与核心功能搭建

### 需求规划阶段

用户以问答方式完成了完整的需求规划（共4轮问答）：

**第1轮 — 项目定位：**
- 核心目标：完整写作工作台（非简单续写工具）
- 运行形式：Web界面
- 多小说管理：统一管理
- 参与度：全程可控（每步需人工确认）

**第2轮 — 技术选型：**
- 技术栈：Python全栈（FastAPI + 轻量前端）
- 功能：4个核心模块全部需要（项目管理、规划生成、正文写作、提示词更新）
- API调用：可配置API层

**第3轮 — 流程细化：**
- 章节规划：可自定义章节数（非固定10章）
- 提示词模板：重新设计结构化架构
- 文风样本：独立管理，按需附加
- 输出格式：纯文本txt

**第4轮 — 功能增强：**
- 章节类型系统：普通/重点/转折，字数分别为基准的100%/130%/150%
- 写作进度统计：仪表盘展示已定稿/已规划章节数
- 审计系统：资源追踪（财富/物品/系统/人物状态/伏笔）+ 冲突检测（6种类型）+ 审计报告
- 结构化提示词拆解为5个模块：基础设定、角色卡片、剧情概述、文风设定、续写方向
- 导入功能：上传已有小说txt → AI分析 → 生成结构化信息
- 章节规划CRUD：可增删改查，可输入方向提示词影响规划
- 修改意见机制：写作后可输入修改意见重新生成

### 开发实施阶段

**创建项目结构：**
```
Novel_Studio2/
├── app/api/        (3个路由文件)
├── app/core/       (7个核心模块)
├── app/models/     (3个数据模型)
├── app/templates/  (1个前端页面)
├── config.yaml
├── requirements.txt
└── run.py
```

**完成的文件清单（共18个）：**

| 文件 | 行数(约) | 说明 |
|------|----------|------|
| `config.yaml` | 18 | API和服务器配置 |
| `requirements.txt` | 8 | Python依赖列表 |
| `run.py` | 12 | 启动脚本 |
| `app/main.py` | 28 | FastAPI主应用，注册路由和模板 |
| `app/core/config.py` | 32 | 配置加载器，路径工具函数 |
| `app/core/api_client.py` | 82 | 可配置的AI API客户端，支持同步和流式调用 |
| `app/core/prompt_builder.py` | 230 | 结构化提示词构建器，6种提示词生成函数 |
| `app/core/importer.py` | 55 | 小说导入分析，调用AI提取结构化信息 |
| `app/core/planner.py` | 52 | 章节规划生成 |
| `app/core/writer.py` | 70 | 正文写作和修改重写 |
| `app/core/auditor.py` | 100 | 审计系统：资源追踪、冲突检测、审计报告 |
| `app/models/novel.py` | 100 | 小说项目模型，含结构化核心提示词 |
| `app/models/chapter.py` | 110 | 章节规划和已写章节模型 |
| `app/models/resource.py` | 100 | 资源追踪器和审计冲突模型 |
| `app/api/novels.py` | 130 | 小说项目管理API（CRUD + 导入 + 提示词 + 文风样本） |
| `app/api/chapters.py` | 200 | 章节规划与写作API（规划CRUD + 写作 + 修改 + 定稿 + 审计） |
| `app/api/audit.py` | 45 | 审计系统API（报告 + 资源 + 解决冲突） |
| `app/templates/index.html` | 600 | 完整Web前端（暗色主题SPA） |

**验证结果：**
- `pip install -r requirements.txt` — 依赖安装成功
- `python -c "from app.main import app"` — 导入验证通过
- `python run.py` — 服务器启动成功
- `curl /api/health` — 返回 `{"status":"ok"}`
- `POST /api/novels` — 创建项目成功
- `GET /api/novels` — 列表返回正确
- `GET /api/novels/{id}/core-prompt` — 结构化提示词读取正确
- `DELETE /api/novels/{id}` — 删除成功
- `GET /` — 前端页面正常加载

**新增文件：**
- `start.bat` — Windows一键启动脚本
- `项目需求.md` — 完整需求文档
- `开发日志.md` — 本文件

### 已知问题

- 暂无

### 待优化

- 核心提示词自动更新（写完一批章节后自动压缩剧情概述）尚未实现
- 流式输出尚未实现（当前为等待完整响应后一次性返回）
- 前端为单HTML文件，后续可能需要拆分优化

---

## 2026-06-01 — 四大功能升级与Bug修复

### 目标
实现四项核心改进：导入创建已定稿章节、流式AI输出、审计markdown渲染、写作独立工作台；修复多个关键Bug。

### 变更内容

**1. 导入创建已定稿章节**
- `app/core/importer.py` — 新增 `split_chapters_regex()` 正则拆分函数，支持"第X章/节/回/卷"、"Chapter X"、数字开头等多种格式，中文数字映射一到五十
- `app/core/importer.py` — 新增 `create_chapters_from_regex()` 创建已定稿章节
- `app/api/novels.py` — 导入端点调用正则拆分，返回 `chapters_created` 数量
- `app/core/prompt_builder.py` — 导入分析提示词移除章节拆分任务（改由正则处理），移除所有输入截断限制

**2. 流式AI输出 (SSE)**
- `app/core/writer.py` — 新增 `write_chapter_stream()` 和 `revise_chapter_stream()` 异步生成器，yield token事件和done事件
- `app/api/chapters.py` — 新增 `POST /write/stream` 和 `POST /{num}/revise/stream` SSE端点
- `app/templates/index.html` — 新增 `apiStream()` 函数用ReadableStream消费SSE

**3. 写作工作台（独立模块）**
- `app/templates/index.html` — 新增第6个tab"写作工作台"，左侧章节计划列表 + 右侧写作输出区 + 底部操作区
- 支持流式写作、流式修改、手动编辑（textarea切换+Ctrl+S保存）、AI调整（针对特定需求修改）
- 内联操作：定稿、审计、导出，无需弹窗
- 从"已写章节"tab的"修改"按钮跳转到写作工作台

**4. 审计Markdown渲染**
- `app/templates/index.html` — 添加marked.js CDN，新增 `.markdown-body` CSS样式
- `loadAuditPanel()` 中资源分类和冲突项用markdown表格/引用块渲染

**5. 角色卡片系统改进**
- `app/templates/index.html` — 折叠模式：默认显示角色名、等级下拉框（可直接编辑）、背景预览
- `app/core/prompt_builder.py` — 导入分析提示词增加角色等级分类标准（main≤5个，大多数secondary/marginal）
- `app/core/prompt_builder.py` — 新增 `plot_importance` 字段要求
- `app/api/novels.py` — AI更新角色提示词增加等级限制和plot_importance要求

**6. 仪表盘改进**
- `app/templates/index.html` — 顶部统计"基准字数/章"改为"累计字数"（显示X.X万）

**7. Bug修复**
- `start.bat` — 从UTF-8转为GBK编码，移除 `chcp 65001`（Windows cmd中文乱码）
- `app/api/novels.py` — 新增 `_strip_ai_preamble()` 函数去除AI响应前导文字（"好的"、"以下是"等）
- `app/api/novels.py` — `update_core_prompt` 端点增加try-except，空 `first_chapter` 自动转0
- `app/templates/index.html` — 添加 `escapeHtml()` 工具函数，所有textarea/input值转义防HTML注入
- `app/templates/index.html` — `savePrompt()` 改用直接fetch，显示具体HTTP错误码
- `app/core/writer.py` — `get_last_chapter_content()` 移除5000字符截断
- `config.yaml` — 新增 `context_window: 1000000` 记录模型上下文能力

### 关键Bug根因

**保存角色卡片后刷新丢失：** 前端 `first_chapter` 为空时传空字符串 `''`，Pydantic v2 无法解析为 `int`，整张角色卡片被静默丢弃。修复：服务端检测空字符串自动转0。

**AI响应带前导文字：** deepseek模型常在正文前加"好的，已根据您提供的..."。修复：prompt强化指令 + `_strip_ai_preamble()` 正则清理。

**导入只识别4章：** AI拆分不可靠。改用正则表达式匹配章节标题，100%可靠。

### 验证结果
- 导入14章txt → 正则拆分创建14个已定稿章节 ✓
- 写作工作台流式输出 → token实时显示 ✓
- 角色卡片折叠编辑+等级下拉 → 保存持久化 ✓
- 审计tab markdown渲染 → 表格/引用块正确显示 ✓

### 当前文件清单（关键变更文件）
| 文件 | 说明 |
|------|------|
| `app/core/importer.py` | 新增正则拆分函数 |
| `app/core/writer.py` | 新增流式写作函数 |
| `app/core/prompt_builder.py` | 移除截断，增强角色分析提示词 |
| `app/api/novels.py` | 导入端点、AI更新、保存修复 |
| `app/api/chapters.py` | 流式SSE端点、手动编辑端点 |
| `app/templates/index.html` | 6-tab SPA、写作工作台、折叠卡片、escapeHtml |
| `config.yaml` | context_window配置 |
| `start.bat` | GBK编码修复 |

### 待优化
- 已写章节tab可考虑增加批量操作（批量定稿、批量审计）
- 核心提示词自动更新（写完一批章节后自动压缩剧情概述）尚未实现
- 前端为单HTML文件（~1200行），后续可考虑拆分

---

## 2026-06-02 — 全面代码审查与23项问题修复

### 目标
对项目所有代码进行全面审查，发现并修复潜在问题（安全、性能、健壮性）。

### 审查范围
共审查17个文件，发现36个问题（5 Critical, 7 High, 13 Medium, 11 Low）。用户选择修复全部 Critical + High + Medium（共23项）。

### 变更内容

**1. 安全修复（Critical）**
- `app/core/config.py` — 新增 `validate_novel_id()` 正则校验 `^[a-zA-Z0-9_-]{1,50}$`，防止路径遍历；`get_novel_dir()` 增加 resolved 路径安全检查
- `app/api/novels.py` — `NovelCreateRequest.id` 增加 Pydantic Field pattern 校验；上传文件名用 `Path(file.filename).name` 清洗；导入和文风样本端点增加 50MB 大小限制
- `app/templates/index.html` — `escapeHtml()` 增加单引号转义 `&#39;`；全面审查所有 innerHTML 注入点，共修复 ~20 处 XSS 风险（标题、内容、描述、冲突信息、资源数据等）
- `app/core/config.py` — 支持 `NOVEL_API_KEY` 环境变量覆盖 config.yaml 中的 api_key

**2. 数据完整性修复（High）**
- `app/models/novel.py` / `app/models/chapter.py` / `app/models/resource.py` — 所有 `save()` 方法改为先写 `.tmp` 临时文件再 `replace()`，防止写入中断导致数据损坏
- `app/models/chapter.py` — `save()` 时清理旧标题的 `.txt` 文件，防止章节改标题后残留旧文件
- `app/models/novel.py` — `update_stats()` 使用具体异常类型 `(json.JSONDecodeError, OSError, KeyError)` 替代裸 `except Exception`；跳过 `.tmp` 文件

**3. 逻辑修复（High）**
- `app/core/api_client.py` — `temperature or self.temperature` 改为 `temperature if temperature is not None else self.temperature`，修复 temperature=0 被忽略的 bug；`max_tokens` 同理
- `app/core/auditor.py` — 删除重复的 `_extract_json()`，改为从 `app/core/importer.py` 导入
- `app/core/api_client.py` — 流式解析增加 `KeyError, IndexError` 捕获，防止异常chunk导致中断

**4. 性能优化（Medium）**
- `app/models/chapter.py` — `list_for_novel()` 新增 `load_content` 参数（默认 False），列表页不读取正文内容
- `app/core/config.py` — `load_config()` 增加 `_CONFIG_CACHE` 缓存，避免每次请求都读文件
- `app/api/chapters.py` — 列表端点调用 `list_for_novel(load_content=False)`

**5. 健壮性修复（Medium）**
- `app/core/importer.py` — `split_chapters_regex()` 用 `pos` 变量跟踪行位置，替代 `novel_text.find(stripped)`，修复重复文本导致位置错误的 bug
- `app/templates/index.html` — `doGeneratePlan()` 在 `closeModal()` 前读取表单值，防止 null 引用
- `app/templates/index.html` — `doAIAdjustInWorkspace()` 完成后更新按钮状态（重新生成/手动编辑/定稿等）
- `app/main.py` — 添加 CORS 中间件（允许 localhost:8000）

**6. 配置管理改进**
- `app/core/config.py` — 新增 `reload_config()` 强制重载函数
- `app/core/api_client.py` — 新增 `reload()` 方法，支持运行时重新加载配置

### 修改文件清单

| 文件 | 变更类型 |
|------|----------|
| `app/core/config.py` | novel_id校验 + 路径安全 + 环境变量 + 配置缓存 |
| `app/core/api_client.py` | or-bug修复 + reload + 流式异常处理 |
| `app/core/auditor.py` | 移除重复_extract_json |
| `app/core/importer.py` | split_chapters_regex位置修复 |
| `app/models/novel.py` | 非原子写入 + update_stats健壮性 |
| `app/models/chapter.py` | 非原子写入 + 旧文件清理 + list_for_novel优化 |
| `app/models/resource.py` | 非原子写入 |
| `app/api/novels.py` | novel_id校验 + 上传安全 + 大小限制 |
| `app/api/chapters.py` | load_content=False |
| `app/main.py` | CORS中间件 |
| `app/templates/index.html` | escapeHtml + ~20处XSS修复 + 按钮状态 + null安全 |

### 验证结果
- 所有模型的 `save()` 方法使用 temp+rename 模式 ✓
- `escapeHtml()` 覆盖所有5种特殊字符 `& " < > '` ✓
- `novel_id` 正则校验 + 路径遍历防护 ✓
- 上传文件名清洗 + 50MB限制 ✓
- `temperature=0` 不再被忽略 ✓
- 配置缓存 + 环境变量覆盖 ✓

### 当前项目状态
- 项目基本稳定，核心功能完整
- 安全防护已到位（路径遍历、XSS、上传安全、API key环境变量）
- 数据写入可靠性提升（非原子写入、旧文件清理）
- 性能优化（列表不加载正文、配置缓存）

### 待优化
- 已写章节批量操作（批量定稿、批量审计）
- 核心提示词自动更新（写完一批章节后自动压缩剧情概述）
- 前端文件拆分（当前~1700行单文件）

---

## 开发日志模板

> 以下模板供后续开发记录使用：

```
## YYYY-MM-DD — 标题

### 目标
本次开发要做什么

### 变更内容
- 修改/新增了哪些文件
- 具体改了什么

### 验证结果
- 测试了什么，结果如何

### 已知问题
- 发现了什么问题

### 待优化
- 后续可以改进的地方
```
